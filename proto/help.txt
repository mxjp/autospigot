
Usage: autospigot <command>

Available Commands:
	init [<target>]
		Create a server environment or restore it's default configuration.
		- target: Optional target directory. Default is the current working directory.

	start [<target>]
		Start a server environment.
		- target: Optional target directory. Default is the current working directory.

	version
		Show autospigot version information.
